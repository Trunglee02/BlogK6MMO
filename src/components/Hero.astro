<section class="hero">
	<div class="hero-content">
		<div class="container">
			<div class="hero-text">
				<h1>Sống Xanh, Sống Ý Nghĩa</h1>
				<p>Khám phá những mẹo hay và thực tế để bắt đầu lối sống bền vững, thân thiện với môi trường. Cùng nhau tạo nên một tương lai xanh cho thế hệ mai sau.</p>
				<div class="hero-buttons">
					<a href="/tips" class="btn btn-primary">Khám Phá Mẹo Hay</a>
					<a href="/about" class="btn btn-secondary">Tìm Hiểu Thêm</a>
				</div>
			</div>
		</div>
	</div>
	<div class="hero-image">
		<img src="https://images.pexels.com/photos/1146706/pexels-photo-1146706.jpeg?auto=compress&cs=tinysrgb&w=1260&h=750&dpr=2" alt="Sustainable living" loading="eager" />
	</div>
</section>

<style>
	.hero {
		display: grid;
		grid-template-columns: 1fr 1fr;
		min-height: 80vh;
		align-items: center;
		background: linear-gradient(135deg, #f0f9ff 0%, #ecfdf5 100%);
		position: relative;
		overflow: hidden;
	}

	.hero-content {
		padding: 4rem 0;
		z-index: 2;
	}

	.hero-text h1 {
		font-size: clamp(2.5rem, 5vw, 4rem);
		font-weight: 700;
		color: #1a202c;
		margin-bottom: 1.5rem;
		line-height: 1.1;
	}

	.hero-text p {
		font-size: 1.25rem;
		color: #4a5568;
		margin-bottom: 2rem;
		max-width: 500px;
		line-height: 1.6;
	}

	.hero-buttons {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
	}

	.btn {
		display: inline-flex;
		align-items: center;
		padding: 1rem 2rem;
		border-radius: 12px;
		font-weight: 600;
		text-decoration: none;
		transition: all 0.3s ease;
		border: 2px solid transparent;
	}

	.btn-primary {
		background: #228B22;
		color: white;
		box-shadow: 0 4px 12px rgba(34, 139, 34, 0.3);
	}

	.btn-primary:hover {
		background: #1a6b1a;
		transform: translateY(-2px);
		box-shadow: 0 6px 20px rgba(34, 139, 34, 0.4);
	}

	.btn-secondary {
		background: transparent;
		color: #228B22;
		border-color: #228B22;
	}

	.btn-secondary:hover {
		background: #228B22;
		color: white;
		transform: translateY(-2px);
	}

	.hero-image {
		position: relative;
		height: 100%;
		min-height: 500px;
	}

	.hero-image img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		border-radius: 20px 0 0 20px;
	}

	@media (max-width: 968px) {
		.hero {
			grid-template-columns: 1fr;
			text-align: center;
		}

		.hero-image {
			order: -1;
			min-height: 300px;
		}

		.hero-image img {
			border-radius: 20px 20px 0 0;
		}

		.hero-content {
			padding: 2rem 0;
		}

		.hero-buttons {
			justify-content: center;
		}
	}

	@media (max-width: 768px) {
		.btn {
			padding: 0.875rem 1.5rem;
			font-size: 0.95rem;
		}

		.hero-buttons {
			flex-direction: column;
			align-items: center;
		}
	}
</style>